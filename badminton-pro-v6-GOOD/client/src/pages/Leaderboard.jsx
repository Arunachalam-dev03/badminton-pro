import React, { useEffect, useState } from 'react'; import api from '../api';
export default function Leaderboard(){ const [players,setPlayers]=useState([]); useEffect(()=>{ (async()=>{ const {data}=await api.get('/players'); setPlayers(data) })() },[]);
  return (<section className="card"><h3 className="font-semibold mb-2">Leaderboard</h3><div className="grid md:grid-cols-3 gap-3">{players.map(p=>(<div className="card flex items-center gap-3" key={p.id}><img className="w-12 h-12 rounded-full object-cover border border-[#20304d]" src={p.photo_url || 'https://via.placeholder.com/48'} /><div><div className="font-semibold">{p.name} {p.country?('• '+p.country):''}</div><div className="text-sm text-[#9fb0cc]">ELO {p.elo} • W{p.wins}-L{p.losses}</div></div></div>))}</div></section>); }
